enum ActiveStatus {
  ACTIVE
  BLOCKED
}

enum Role {
  TENANT
  LANDLORD
  ADMIN
}

enum PropertyStatus {
  AVAILABLE
  RENTED
  UNAVAILABLE
}

enum RentalRequestStatus {
  PENDING // Tenant submitted, waiting for landlord
  APPROVED // Landlord approved, awaiting payment
  REJECTED // Landlord rejected
  ACTIVE // Payment completed, tenant moved in
  COMPLETED // Rental period finished (tenant can now review)
}

enum PaymentStatus {
  PENDING
  COMPLETED
}
